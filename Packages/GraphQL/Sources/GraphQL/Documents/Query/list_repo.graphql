query ListRepo(
  $after: String
  $before: String
  $first: Int
  $last: Int
  $query: String!
) {
  search(
    after: $after
    before: $before
    first: $first
    last: $last
    query: $query
    type: REPOSITORY
  ) {
    codeCount
    discussionCount
    edges {
      cursor
      node {
        ... on Repository {
          description
          homepageUrl
          nameWithOwner
          owner {
            avatarUrl
          }
          stargazers {
            totalCount
          }
          primaryLanguage {
            name
          }
        }
      }
    }
    issueCount
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
    }
    repositoryCount
    userCount
    wikiCount
  }
}

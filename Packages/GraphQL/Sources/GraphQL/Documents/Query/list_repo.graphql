query ListRepo(
  $after: String
  $first: Int
  $query: String!
) {
  search(
    after: $after
    first: $first
    query: $query
    type: REPOSITORY
  ) {
    edges {
      cursor
      node {
        ... on Repository {
          id
          url
          description
          homepageUrl
          nameWithOwner
          owner {
            id
            avatarUrl
            login
            resourcePath
            url
          }
          stargazerCount
          primaryLanguage {
            id
            name
            color
          }
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
    }
    repositoryCount
  }
}
